# Project Guidelines (SSoT)

## Tech Stack
- **フロントエンド**: React(推奨), JavaScript(ES6+), CSS Modules(またはStyled Components)
- **ホスティング**: Cloudflare Pages
- **バックエンド API**: Cloudflare Pages Functions
- **データベース**: Cloudflare D1
- **オブジェクトストレージ**: Cloudflare R2 (画像・動画)
- **ユーザー認証**: OAuth 2.0 (Google/OAuth Provider)を必須とし、自前でのパスワード管理は行わない。

## 参照先（厳守 - SSoT）
> **重要**: これらのファイルはプロジェクトの Single Source of Truth（唯一の情報源）です。
> AI は常にこれを参照し、厳守する必要があります。

- **行動規範:** `.agents/rules/` 内の全ファイルを**必ず**読み、厳守してください。
  - タスクの中断・再開始時
  - 環境切り替え時
  - ファイル編集前
  - コミットメッセージ生成時
  
- **ワークフロー:** `.agents/workflows/` 内の全ファイルを**必ず**読み、指示されたフローに従ってください。
  - トリガー（`/plan`, `/commit` 等）に応じた適切なフローを実行

> ⚠️ **違反時の影響**: 参照しないことは、プロジェクトの品質・整合性を損ないます。
> 内容が変更された場合は、本ファイルの参照先も即時更新してください。

## work_log.md更新ルール
毎回の作業後、 `work_log.md` に次の内容を**追記**してください。
無ければ、ファイルを作成してください。
- 作業日
- 実施テーマ
- 作成したファイル
- 更新したファイル
- AIが行った作業
- 利用者が入力した主な内容
- 人間が確認すべき点
- GitHubコミットメッセージ案

## セキュリティと倫理
次の情報は、HTML、Markdown、GitHubに含めないでください。
- 氏名と連絡先の組み合わせ
- 住所
- 電話番号
- メールアドレス
- 学籍番号
- 社員番号
- パスワード
- APIキー
- 顧客情報
- 成績情報
- 社内資料
- 未公開資料
- 他人の個人情報
判断に迷う場合は、利用者に確認してください。

## 厳守ルール
AIが何かをする場合は、以下の内容を**絶対に**厳守してください。

- `.agents/rules/` 内に格納された行動規範すべて
- `.agents/workflows/` 内に格納されたワークフローすべて